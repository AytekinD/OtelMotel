@model ICollection<Role>

<a asp-action="Create" asp-controller="Role" class="btn btn-primary">Role Ekle</a>
<div class="card">
    <div class="car-header text-center">
        <h5><b>Roller</b></h5>
    </div>
    <div class="card-body">
        <table class="table table-hover table-secondary">
            <thead>
                <tr>
                    <td>Role Name</td>
                    <td>Status</td>
                    <td></td>
                    <td></td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.RoleName</td>
                        <td>@item.Status</td>
                        <td><a asp-action="Update" asp-controller="Musteri" asp-route-Id="@item.Id" class="btn btn-outline-success ">Update</a></td>
                        <td><a asp-action="Delete" asp-controller="Musteri" asp-route-Id="@item.Id" class="btn btn-outline-danger">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>